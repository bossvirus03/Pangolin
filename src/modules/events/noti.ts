export default class NotiCommand {
  static config = {
    name: "noti",
    version: "1.0.0",
    author: "loi",
    createdAt: "",
  };

  run(api, event, client, args) {
    // console.log(event);
  }

  onload(api, client) {}
}
